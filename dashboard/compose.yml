services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    # No volume for fresh DB on each start
    environment:
      - SQLITE_DB_PATH=/app/data/app.db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend