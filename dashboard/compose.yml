services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data  # Persist SQLite DB
    environment:
      - SQLITE_DB_PATH=/app/data/app.db

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend