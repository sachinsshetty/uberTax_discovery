services:
  server:
    image: dwani/ubertax-server:latest
    ports:
      - "80:18889"
    environment:
      - SQLITE_DB_PATH=/app/data/app.db
      - DWANI_API_BASE_URL=https://test-api.dwani.ai
    restart: unless-stopped